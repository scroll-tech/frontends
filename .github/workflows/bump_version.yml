name: "Bump Version"

on:
  push:
    branches:
      - "test-version-bump"

jobs:
  bump-version:
    name: "Bump Version on main"
    permissions:
      contents: write
    runs-on: ubuntu-latest

    steps:
      - name: "Checkout source code"
        uses: "actions/checkout@v3"
        with:
          ref: ${{ github.ref }}
      - name: "cat package.json"
        run: cat ./package.json
      - name: "Automated Version Bump"
        id: version-bump
        uses: "phips28/gh-action-bump-version@25cd8ffb8ec95f16f83a8dac9557a845e4fa9e21"
        with:
          tag-prefix: "v"
          skip-push: "true"
        env:
          GITHUB_TOKEN: ${{ secrets.WRITE_TOKEN }}
      # - run: git push -v --force origin HEAD\:_ci-bump-version
      - run: echo "::set-output name=title::$(git log --pretty=format:"%s" -1)"
        id: get-bump-version-commit-message
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@2b011faafdcbc9ceb11414d64d0573f37c774b04
        with:
          token: ${{ secrets.WRITE_TOKEN }}
          delete-branch: false
          branch: ci/_bump-version
          title: ${{ steps.get-bump-version-commit-message.outputs.title }}
          labels: "bot/bump-version, automerge"
          body: ""
