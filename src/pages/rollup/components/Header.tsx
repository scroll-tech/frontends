import PageHeader from "@/components/PageHeader"

const Header = () => {
  return <PageHeader title="Rollup Explorer" subTitle="Track the status of blocks and transactions as they are committed and finalized."></PageHeader>
}

export default Header
